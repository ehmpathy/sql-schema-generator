export enum DataTypeName {
  CHAR = 'char',
  VARCHAR = 'varchar',
  TEXT = 'text',
  ENUM = 'enum',

  INT = 'int',
  BIGINT = 'bigint',

  DATETIME = 'datetime',
}

export enum Constraint {
  FOREIGN_KEY = 'FOREIGN_KEY',
}
