export enum DataTypeName {
  CHAR = 'char',
  VARCHAR = 'varchar',
  TEXT = 'text',
  ENUM = 'enum',

  INT = 'int',
  BIGINT = 'bigint',

  DATETIME = 'datetime',

  BINARY = 'binary',
}

export enum Constraint {
  FOREIGN_KEY = 'FOREIGN_KEY',
}
