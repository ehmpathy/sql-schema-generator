# 2.detect.hazards output

## status

hazards detected and resolved during upgrade.

## hazards identified

### path hazards

1. **src/domain/ -> src/domain.objects/**
   - what: folder renamed
   - why it may break: imports use `@src/domain`
   - status: RESOLVED - updated 67 files via sedreplace
   - verified: types pass

2. **src/logic/ -> src/domain.operations/**
   - what: folder renamed
   - why it may break: imports use `@src/logic`
   - status: RESOLVED - declapract handled automatically
   - verified: types pass

3. **src/__test_utils__/ -> src/.test.utils/**
   - what: folder renamed
   - why it may break: relative imports in test utilities
   - status: RESOLVED - updated 6 files manually
   - verified: integration tests pass

4. **src/__nonpublished_modules__/ -> src/_topublish/**
   - what: folder renamed
   - why it may break: imports use old path
   - status: RESOLVED - declapract handled automatically
   - verified: types pass

5. **acceptance/ -> blackbox/**
   - what: folder renamed
   - why it may break: test config references
   - status: RESOLVED - declapract updated configs
   - verified: no acceptance tests in this repo

### config hazards

1. **eslint/prettier -> biome**
   - what: linter and formatter changed
   - why it may break: different rules, different output
   - status: RESOLVED - added override for oclif default exports
   - verified: `npm run fix` passes

2. **npm -> pnpm**
   - what: package manager changed
   - why it may break: lockfile format, install behavior
   - status: RESOLVED - pnpm-lock.yaml created
   - verified: `pnpm install` succeeds

### test hazards

none - all tests pass:
- types: passed
- unit: 105 passed
- integration: 78 passed

### cicd hazards

1. **workflow changes**
   - what: github actions workflows updated
   - why it may break: different syntax, different actions
   - status: TO VERIFY - will verify after push to origin
   - plan: push branch and check CI status

## summary

all local hazards resolved. cicd hazards to be verified after push.
