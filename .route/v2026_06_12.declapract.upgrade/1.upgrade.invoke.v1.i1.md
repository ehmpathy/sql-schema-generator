# 1.upgrade.invoke output

## status

upgrade completed successfully.

## summary

- upgraded declapract-typescript-ehmpathy to 0.47.75 (pinned to avoid broken 0.48.x)
- migrated from eslint/prettier to biome
- migrated from npm to pnpm
- renamed directories per new conventions:
  - `src/domain/` -> `src/domain.objects/`
  - `src/logic/` -> `src/domain.operations/`
  - `src/__test_utils__/` -> `src/.test.utils/`
  - `src/__nonpublished_modules__/` -> `src/_topublish/`
  - `acceptance/` -> `blackbox/`
  - `src/contract/__test_assets__/` -> `src/contract/.test.assets/`

## fixes applied

1. fixed absent exports in `src/domain.objects/index.ts` - added exports for Entity, Property, Literal, DataType, Event, GeneratorConfig
2. added biome override for oclif commands (require default export)
3. fixed import paths from `@src/domain` to `@src/domain.objects` (67 files)
4. fixed import paths for `.test.utils` directory (6 files)

## tests

- types: passed
- unit: 105 passed
- integration: 78 passed
